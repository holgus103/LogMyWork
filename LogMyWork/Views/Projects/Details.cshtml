@model LogMyWork.Models.Project

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Project</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <h5>Tasks</h5>
        @foreach(var task in Model.Tasks)
        {
            <dt>
                @Html.DisplayNameFor(tmp => task.Name)
            </dt>
            <dd>
                @Html.DisplayFor(tmp => task.Name)
                @*@Html.ActionLink("Edit", "Edit", new { id = task.TaskID }) |
                @Html.ActionLink("Details", "Details", new { id = task.TaskID }) |
                @Html.ActionLink("Delete", "Delete", new { id = task.TaskID }) |
                @Html.ActionLink("Track", "Track", new { id = task.TaskID })*@
            </dd>
        }
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ProjectID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
